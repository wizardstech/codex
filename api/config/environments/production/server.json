{
  "host": "${process.env.APP_HOST}",
  "port": "${process.env.PORT}",
  "production": true,
  "proxy": {
    "enabled": false,
    "ssl": true
  },
  "autoReload": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "path": "/dashboard"
  }
}
